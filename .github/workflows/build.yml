# Audit branch only. The production workflow is unchanged on the PR branch.
name: Build
on: workflow_dispatch
permissions:
  contents: read
jobs:
  audit-only:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Use PR 327 isolated audit on this temporary branch.'
